[package]
name = "ddd-toolkit-core"
version = "0.0.0"
edition = "2024"

[dependencies]
trait-variant = "0.1.2"
uuid = { version = "1.24.0", optional = true, features = ["v4", "v7"] }

[features]
uuid = ["dep:uuid"]
